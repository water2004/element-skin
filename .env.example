# Element-Skin 环境变量配置
# 复制此文件为 .env 并根据实际情况修改

# ==================== 后端配置 ====================

# JWT 密钥（⚠️ 生产环境务必修改为随机强密钥！）
# 可使用以下命令生成：openssl rand -base64 32
JWT_SECRET=your-production-secret-key-change-me

# 数据库路径（容器内路径，对应挂载的 ./data 目录）
# DATABASE__PATH=/data/yggdrasil.db

# 材质存储目录（容器内路径）
# TEXTURES__DIRECTORY=/data/textures

# 服务器监听配置
# SERVER__HOST=0.0.0.0
# SERVER__PORT=8000

# ==================== 前端配置 ====================

# 前端部署基础路径（默认为根目录）
# 如需部署到子目录，设置为 /subdir/（必须以 / 开头和结尾）
# 示例：
#   根目录部署: VITE_BASE_PATH=/
#   子目录部署: VITE_BASE_PATH=/skin/
# 注意：修改此值后需要重新构建前端镜像（docker compose build --no-cache frontend）
VITE_BASE_PATH=/

# API 基础 URL（通常留空，使用相对路径）
# 仅在以下情况需要设置：
#   1. 前端和后端不在同一域名
#   2. 后端也部署在子目录下
# 示例：
#   同域名: VITE_API_BASE=
#   不同域名: VITE_API_BASE=https://api.yourdomain.com
#   后端子目录: VITE_API_BASE=/api
VITE_API_BASE=

# ==================== Docker 配置 ====================

# 容器时区
TZ=Asia/Shanghai

# 日志级别
LOG_LEVEL=INFO

# ==================== 可选配置 ====================

# 如使用外部数据库，可配置连接字符串
# DATABASE_URL=postgresql://user:password@localhost/dbname

# 如需邮件通知功能，配置 SMTP
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourdomain.com

# Redis 配置（如需缓存功能）
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Sentry 错误追踪（可选）
# SENTRY_DSN=https://xxx@sentry.io/xxx

# ==================== 说明 ====================
# 
# 1. 环境变量使用双下划线表示层级结构
#    例如：JWT__SECRET 对应 config.yaml 中的 jwt.secret
#
# 2. 优先级：环境变量 > config.yaml > 默认值
#
# 3. 运营配置（站点名称、速率限制等）建议在管理面板修改
#
# 4. 生产环境务必修改：
#    - JWT_SECRET
#    - 管理员密码
#    - 数据库密码（如使用外部数据库）
